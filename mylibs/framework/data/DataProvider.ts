import { IUIElement } from "carbon-model";

export default class DataProvider {
    fetch(fields, rowCount){
    }
    getConfig(){
    }
    createElement(field, templateId): IUIElement{
        return null;
    }
}